{parse} = import '@fink/larix'
{generate} = import '..'


fink2js = fn strings, ...exprs:
  source = String.raw:: {raw: strings}, ...exprs
  filename = 'test.fnk'
  [ast, {errors}] = parse:: source, filename
  {code} = generate:: ast, filename, source
  code

