{regExpLiteral} = import '@babel/types'
{replace} = import '@fink/std-lib/str'


transform_regex = fn node:
  pattern = replace node.value, rx/(\#.*\n)|[\n\s]/gm, ''

  regExpLiteral pattern, node.flags
