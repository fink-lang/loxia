{binaryExpression} = import '@babel/types'


transform_op = {
  '==': '===',
  '!=': '!==',
  '^': '**'
}


transform_binary = fn node, {transform}:
  left = transform:: node.left
  right = transform:: node.right

  op = transform_op.(node.op) || node.op

  binaryExpression:: op, left, right
