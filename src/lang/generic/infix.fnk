{callExpression} = import'@babel/types'
{ident} = import '../../js/types'


transform_inifx = fn node, {transform}:
  left = transform:: node.left
  right = transform:: node.right

  callExpression::
    ident:: node.op
    [left, right]

