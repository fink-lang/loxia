{fink2js} = import '../../testing'
{describe, it, expect, to_match_snapshot} = import '@fink/jest'


describe 'pipe', fn:
  it 'compiles', fn:

    expect
      fink2js`
        pipe:
          foo
          bar
          spam

        pipe [1, 2, 3]:
          map item: item * 2
      `
      to_match_snapshot

