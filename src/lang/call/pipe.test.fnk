{fink2js} = import '../../testing/index.fnk'
{describe, it, expect, to_match_snapshot} = import '@fink/jest/test.fnk'


describe 'pipe', fn:
  it 'compiles', fn:

    expect
      fink2js '
        pipe:
          foo
          bar ?, {foo, ...?}
          spam
          [...?]


        pipe [1, 2, 3]:
          map item: item * 2
      '
      to_match_snapshot

