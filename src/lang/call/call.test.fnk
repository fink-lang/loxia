{fink2js} = import '../../testing'
{describe, it, eq, snapshot} = import '../../testing/jest'


describe:: 'call', fn:
  it:: 'compiles', fn:

    fink2js(`
      call1 = a(ni, x=123, ...x)
      call2 = a(ni)
      call3 = a ()
      call4 = a(fn x: x*2)
    `) eq snapshot



