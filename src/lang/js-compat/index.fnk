{add, any} = import '../context'
{transform_new} = import './new'
{transform_throw} = import './throw'


add_js_compat = fn ctx:
  pipe ctx:
    add('new', any, transform_new)
    add('throw', any, transform_throw)
