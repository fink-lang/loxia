babel_types = import '@babel/types'
{unaryExpression} = babel_types
{wrap} = import '../../js/types.fnk'



transform_throw = fn node, {transform}:
  right = transform node.right

  wrap node, unaryExpression 'throw', right
