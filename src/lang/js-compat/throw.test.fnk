{fink2js} = import '../../testing'
{describe, it, eq, snapshot} = import '../../testing/jest'


describe:: 'throw', fn:
  it:: 'compiles', fn:
    fink2js(`
      fn bar:
        match true:
          has_err(bar): throw err('spam')
          else: bar
    `) eq snapshot


