{add, any} = import '../context.fnk'



transform_group = fn node, {transform}:
  # TODO: what if group has multiple expr
  [expr] = node.exprs
  transform expr



add_group = fn ctx:
  pipe ctx:
    add 'group', any, transform_group
