babel_types = import '@babel/types'
{spreadElement} = babel_types
{add, any} = import '../context.fnk'


transform_spread = fn node, {transform}:
  right = transform node.right
  spreadElement right


add_spread = fn ctx:
  pipe ctx:
    add any, '...', transform_spread
