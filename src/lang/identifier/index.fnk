{identifier} = import '@babel/types'

{escape_ident} = import '../../js/identifier'
{add, any} = import '../context'


transform_ident = fn {value}:
  name = escape_ident(value)
  identifier(name)


add_ident = fn ctx:
  pipe ctx:
    add('ident', any, transform_ident)
