{awaitExpression} = import '@babel/types'
{add, any} = import '../context.fnk'


transform_await = fn node, {transform}:
  right = transform node.right
  awaitExpression right


add_async = fn ctx:
  pipe ctx:
    add 'await', any, transform_await

