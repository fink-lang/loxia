{fink2js} = import '../../testing'
{describe, it, expect, to_match_snapshot} = import '@fink/jest'


describe:: 'logical', fn:
  it:: 'compiles operators', fn:

    expect::
      fink2js`
        logical = a || b || c && d < 1 <= s
      `
      to_match_snapshot



